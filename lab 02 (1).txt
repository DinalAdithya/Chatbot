CREATE TYPE emp_t AS OBJECT(
	EMPNO CHAR(6),
	FIRSTNAME VARCHAR(12),
	LASTNAME VARCHAR(15),
	WORKDEPT REF dept_t,
	SEX CHAR(1),
	BIRTHDAY DATE,
	SALARY NUMBER(8,2)
)/

CREATE TYPE dept_t AS OBJECT(
	DEPTNO CHAR(3),
	DEPTNAME VARCHAR(36),
	MGRNO REF emp_t,
	ADMRDEPT dept_t,
	
)
/

####### ALTER TABLE
####### UPDATE CELL
####### RUN commit in the end to commit

CREATE TABLE OREMP OF emp_t(
	CONSTRAINT OREMP_PK PRIMARY KEY(EMPNO),
	CONSTRAINT OREMP_FIRSTNAME_NN FIRSTNAME NOT NUL,
	CONSTRAINT OREMP_LASTNAME_NN LASTNAME NOT NULL,
	CONSTRAINT OREMP_SEX_CK CHECK SEX
	
	
	
)
/

CREATE TABLE ORDEPT as dept_t(
	
	
	
)
/

INSERT INTO OREMP VALUES(dept_t('A00','SPIFFY...',NULL,NULL))

INSERT INTO OREMP VALUES(dept_t('B00','Planing',NULL,(
SELECT ref(d)
FROM ORDEPT.d
WHERE DEPTNO = 'A00'
)))


***********
SELECT ref(d.)
FROM ORDEPT.d







